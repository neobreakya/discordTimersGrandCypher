[phases.setup]
nixPkgs = ['nodejs_18']

[phases.install]
cmds = ['npm install']

[phases.build]

[start]
cmd = 'node bot.js'
```

---

## Also create a Procfile (Railway fallback)

Create a file called `Procfile` (no extension) in your project root:
```
web: node bot.js